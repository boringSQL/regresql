policies:
  severity:
    sequential_scan_detected: info
    multiple_sequential_scans: info
  reasons:
    sequential_scan_detected: >
      A seq scan on a blog-sized table is almost always the right plan.
